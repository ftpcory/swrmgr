#!/usr/bin/env bash
set -euo pipefail
. "${SWRMGR_ROOT:-/opt/swrmgr}/lib/core.sh"

stack="$(stack_name "${1}")"

aws secretsmanager \
  create-secret \
    --name "swrmgr/customer/${stack}/env.json" \
    --secret-string '{}' \
    --tags "Key=stack,Value=${stack}"
